language: python

sudo: required
dist: trusty

install:
  - sudo apt-get install -y
      wget
      python-dev
      python-pip
      cmake
      g++
  - pip install psutil

script:
  - python start.py
